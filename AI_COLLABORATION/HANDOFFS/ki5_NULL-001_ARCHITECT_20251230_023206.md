---
status: COMPLETE
task: NULL-001
role: ARCHITECT
phase: ARCHITECT
files_changed: []
summary:
  - Define new module kenobase/analysis/null_models.py for permutation-based null models
  - Implement schedule_permutation() preserving draw-schedule structure (same weekdays)
  - Implement block_permutation() preserving temporal blocks (weekly autocorrelation)
  - Add NullModelRunner class integrating with existing NullModelType enum in axioms.py
  - Add FDR-corrected p-value computation for each null model type
  - Create unit tests in tests/unit/test_null_models.py with synthetic data validation
  - Integrate null models with existing axiom predictions (P1.3, P4.3, P6.3, P7.3)
---

# Rule Confirmation
- Rule 1 (SYSTEM_STATUS + ADR/Docs + git status): CONFIRMED
- Rule 2 (granularity stated): per-prediction (each axiom prediction uses specific null model)
- Rule 3 (semantics defined): NullModelType enum, p_value, q_value (FDR), n_permutations
- Rule 4 (target metric): p-value significance vs null model
- Rule 5 (helper-only boundaries): CONFIRMED
- Rule 6 (reproducibility): `pytest tests/unit/test_null_models.py -v` -> stdout or UNVERIFIED

## Task Setup
- Granularity: per-prediction (P1.3, P4.3, P6.3, P7.3 use BLOCK/SCHEDULE null models)
- Semantics: `NullModelType.BLOCK_PERMUTATION`, `NullModelType.SCHEDULE_PRESERVING` from `kenobase/core/axioms.py:23-24`
- Target metric: Empirical p-value computed from permutation distribution

## Repro Commands
- `pytest tests/unit/test_null_models.py -v` -> stdout (after implementation)

---

**Handoff erstellt:** `AI_COLLABORATION/HANDOFFS/ki5_NULL-001_ARCHITECT_20251230_023206.md`

**Naechste Phase:** PROXY → EXECUTOR
