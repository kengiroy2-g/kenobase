---
status: COMPLETE
task: HYP003-001
role: ARCHITECT
phase: ARCHITECT
files_changed: []
summary:
  - Scraper infrastructure ALREADY EXISTS in kenobase/scraper/ (4 modules)
  - 16 Landeslotterien configs defined in landeslotterien.py with CSS selectors
  - PressReleaseScraper (base.py) handles rate limiting, retries, session mgmt
  - KenoWinnerParser (parsers.py) extracts numbers, amounts, locations, dates
  - CLI script scripts/scrape_press.py ready for execution
  - No new implementation needed - TASK IS ESSENTIALLY COMPLETE
  - EXECUTOR should VALIDATE existing code and RUN test scrape
  - Lotto Hessen API endpoint also documented (services.lotto-hessen.de)
---

# Rule Confirmation
- Rule 1 (SYSTEM_STATUS + ADR/Docs + git status): CONFIRMED
- Rule 2 (granularity stated): per-bundesland
- Rule 3 (semantics defined): KenoWinnerRecord(bundesland, city, region, numbers, keno_type, amount_eur, draw_date)
- Rule 4 (target metric): data-collection (N extracted records with confidence > 0.3)
- Rule 5 (helper-only boundaries): CONFIRMED
- Rule 6 (reproducibility): `python scripts/scrape_press.py --sites bayern --verbose` -> data/scraped/keno_winners_*.json

## Repro Commands
```powershell
python scripts/scrape_press.py --sites bayern --verbose
python scripts/scrape_press.py --all --output data/scraped
python scripts/scrape_press.py --list
```

# Implementierungsplan

**Befund:** Komplette Scraper-Infrastruktur existiert bereits in `kenobase/scraper/`.

| Modul | Pfad | Status |
|-------|------|--------|
| Landeslotterien Config | kenobase/scraper/landeslotterien.py | VOLLSTAENDIG (16 Lotterien) |
| Base Scraper | kenobase/scraper/base.py | VOLLSTAENDIG |
| Parsers | kenobase/scraper/parsers.py | VOLLSTAENDIG |
| CLI Script | scripts/scrape_press.py | VOLLSTAENDIG |

**EXECUTOR Checkliste:**
1. Dependencies pruefen (requests, beautifulsoup4)
2. Import-Test: `python -c "from kenobase.scraper import PressReleaseScraper"`
3. Test-Scrape: `python scripts/scrape_press.py --sites bayern --verbose`
4. JSON-Output validieren

**Handoff-Datei:** `AI_COLLABORATION/HANDOFFS/ki1_HYP003-001_ARCHITECT_20251228_160633.md`
