---
status: COMPLETE
task: TASK-H02
role: EXECUTOR
phase: EXECUTOR
files_changed:
  - kenobase/analysis/stable_numbers.py (NEW)
  - kenobase/analysis/__init__.py (MODIFIED)
  - scripts/analyze.py (MODIFIED)
  - tests/unit/test_stable_numbers.py (NEW)
summary:
  - Created kenobase/analysis/stable_numbers.py with Model Law A implementation
  - StableNumberResult dataclass with number, stability_score, is_stable, avg_frequency, std_frequency
  - Stability formula: stability_score = 1 - (std / mean), clamped to [0, 1]
  - analyze_stable_numbers() uses calculate_rolling_frequency from frequency.py
  - Config integration: uses physics.stability_threshold (default 0.90)
  - CLI: new `stable-numbers` command with --window, --threshold, --all options
  - Unit tests: 19 tests, all passing
  - Exports added to kenobase/analysis/__init__.py
---
# Rule Confirmation
- Rule 1 (SYSTEM_STATUS + ADR/Docs + git status): CONFIRMED
- Rule 2 (granularity stated): per-number
- Rule 3 (semantics defined): stability_score = 1 - (std/mean), is_stable = score >= threshold
- Rule 4 (target metric): stability >= 0.90 (from physics.stability_threshold)
- Rule 5 (helper-only boundaries): CONFIRMED
- Rule 6 (reproducibility): `python scripts/analyze.py stable-numbers -d data/raw/keno/KENO.csv -o results/stable_numbers_keno.json`

## Task Setup
- Granularity: per-number
- Semantics: stability_score = 1 - (std(rolling_freq) / mean(rolling_freq))
- Target metric: stability >= physics.stability_threshold (0.90)

## Repro Commands
- `python scripts/analyze.py stable-numbers -d data/raw/keno/KENO.csv -o results/stable_numbers_keno.json`
- `python -m pytest tests/unit/test_stable_numbers.py -v` -> 19 passed

# Umsetzung

**Handoff erstellt:** `AI_COLLABORATION/HANDOFFS/ki2_TASK-H02_EXECUTOR_20251227_220747.md`
