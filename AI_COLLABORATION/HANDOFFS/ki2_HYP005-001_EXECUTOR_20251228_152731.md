---
status: COMPLETE
task: HYP005-001
role: EXECUTOR
phase: EXECUTOR
files_changed:
  - scripts/analyze_index_reset.py (NEW)
  - tests/unit/test_number_index.py (NEW)
summary:
  - Created scripts/analyze_index_reset.py for HYP-005 Index-Reset analysis
  - Created tests/unit/test_number_index.py with 18 unit tests (all passing)
  - Script loads KENO draws + GK1 events via DataLoader
  - Uses existing calculate_index_correlation() from number_index.py
  - Output format: JSON with full statistical results
  - Ran successfully: p=0.0419 < 0.05 -> SUPPORTED
  - Acceptance Criteria MET: Index shows significant predictive power
---
# Rule Confirmation
- Rule 1 (SYSTEM_STATUS + ADR/Docs + git status): CONFIRMED
- Rule 2 (granularity stated): per-number
- Rule 3 (semantics defined): Index = Erscheinungshaeufigkeit seit letztem GK1-Reset
- Rule 4 (target metric): p-Value < 0.05
- Rule 5 (helper-only boundaries): CONFIRMED
- Rule 6 (reproducibility): `python scripts/analyze_index_reset.py` -> `results/hyp005_index_reset.json`

## Task Setup
- Granularity: per-number (70 Zahlen)
- Semantics: Index = count(Erscheinungen) seit letztem GK1-Event
- Target metric: t-Test p-Value

## Repro Commands
- `python scripts/analyze_index_reset.py --data-path data/raw/keno/KENO_ab_2018.csv --gk1-path Keno_GPTs/10-9_KGDaten_gefiltert.csv --output results/hyp005_index_reset.json`
- `python -m pytest tests/unit/test_number_index.py -v` (18/18 passing)

# Umsetzung

## 1. Script erstellt: scripts/analyze_index_reset.py

CLI-Script fuer HYP-005 Index-Reset Analyse:
- Laedt KENO-Ziehungen via DataLoader (GameType.KENO)
- Laedt GK1-Events via DataLoader (GameType.GK1_SUMMARY)
- Ruft calculate_index_correlation() auf
- Gibt formatierte Konsolen-Ausgabe
- Speichert JSON-Ergebnis

## 2. Unit Tests erstellt: tests/unit/test_number_index.py

18 Tests in 5 Klassen, alle PASSED.

## 3. Analyse-Ergebnis

- p-Value: **0.0419** (< 0.05)
- Verdict: **SUPPORTED**

---

**Handoff erstellt:** `AI_COLLABORATION/HANDOFFS/ki2_HYP005-001_EXECUTOR_20251228_152731.md`
