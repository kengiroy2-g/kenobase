---
status: REJECTED
task: TRANS-001
role: PROXY
phase: PROXY_IMPL
reviewed_handoff: "ki2_TRANS-001_EXECUTOR_20251230_034109.md"
summary:
  - Disabled Summen-Signatur path crashes because `summen_signatur_buckets`/`summen_signatur_path` stay undefined when the feature flag is off; any run with `analysis.summen_signatur.enabled=False` raises UnboundLocalError (kenobase/pipeline/runner.py:278-337).
  - Ran sanity checks: py_compile on module/CLI/runner and targeted pytest all pass.
  - KI profile `AI_COLLABORATION/KI_PROFILES/ki0_proxy.md` missing; unable to cross-check known bugs; CURRENT_STATUS still unchanged.
  - Filed proxy handoff with REJECTED status at `AI_COLLABORATION/HANDOFFS/ki0_TRANS-001_PROXY_IMPL_20251230_035820.md`.
---
# Rule Confirmation
- Rule 1 (SYSTEM_STATUS + ADR/Docs + git status): UNVERIFIED (SYSTEM_STATUS.md read; ki0_proxy profile missing)
- Rule 2 (granularity stated): per-market (per KENO type buckets)
- Rule 3 (semantics defined): sum_total, sum_scaled, sum_bucket, parity_vector, decade_hist, checksum (sha256)
- Rule 4 (target metric): calibration
- Rule 5 (helper-only boundaries): CONFIRMED
- Rule 6 (reproducibility): python -m pytest tests/unit/test_summen_signatur.py -q -> PASS

## Task Setup
- Granularity: per-market (per KENO type buckets)
- Semantics: sum_total, sum_scaled, sum_bucket, parity_vector, decade_hist, checksum (sha256)
- Target metric: calibration

## Repro Commands
- python -m py_compile kenobase/analysis/summen_signatur.py scripts/compute_summen_signatur.py kenobase/pipeline/runner.py -> PASS (no output)
- python -m pytest tests/unit/test_summen_signatur.py -q -> PASS

# Proxy Review (Implementation)
- Major: `analysis.summen_signatur.enabled=False` leaves `summen_signatur_buckets`/`summen_signatur_path` uninitialized before the PipelineResult return, so any run with the feature disabled will error despite the guard; initialize to None ahead of the flag check to keep the flag workable (kenobase/pipeline/runner.py:278-337).
- Note: KI profile `AI_COLLABORATION/KI_PROFILES/ki0_proxy.md` not present, so known-bug cross-check not possible; CURRENT_STATUS.md untouched pending standard status update.